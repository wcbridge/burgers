<li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}} TIME!{{else}}Eat!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
